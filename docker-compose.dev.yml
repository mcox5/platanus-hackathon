services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    # The following ensures ENVIRONMENT is always set to production for this container
    # regardless of what's in .env
    environment:
      - ENVIRONMENT=production
